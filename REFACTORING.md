# リファクタリング計画

最終更新: 2026-09-15 / 初回調査対象: `3d02737`

## 現在の進捗

| 項目 | 状態 | 残作業 |
| --- | --- | --- |
| 1. 自動検証基盤 | 実装・ローカル検証完了、`4340ed3`でpush済み | GitHub上のCI実行結果・必須チェック設定の確認 |
| 2. 写真保存・削除の失敗対策 | 公開前状態、保存予定キーの永続化、中断投稿の手動削除まで実装、未コミット | 重複送信、自動再試行、稼働中アップロードと削除の競合 |
| 3. DB定義とテストDBの統一 | 実装・ローカル検証完了、未コミット | 本番DBの実構造・手動適用履歴の照合と適用判断 |
| 11. 未使用コード整理 | 旧トップ・クラゲ関連9ファイルの削除完了、未コミット | 未使用依存の整理、実験ページの位置付け |
| 12. 設定・文書 | 一部完了 | API設定名、出力ディレクトリ、素材パス等の整理 |
| 4〜10 | 未着手 | 各項目の計画を参照 |

項目2は保存中の投稿・画像を非公開にし、中断した投稿も管理画面から削除可能。重複送信と自動回復は残作業。本番DBの照合および0005・0006の適用も別の運用作業として残す。

## 実施状況

2026-09-15: 0006で投稿の公開状態・アップロード識別子・保存予定キーを追加。既存投稿は公開を維持し、新規投稿は全画像保存後に公開する。公開一覧・詳細・画像配信から保存中の投稿を除外。初回INSERTの応答喪失時は識別子から投稿IDを回復し、公開更新の応答喪失時はDBを確認して成功扱いにする。中断投稿の削除は保存予定キーも対象とする。テストとローカルmigrationの検証結果は以下の項目2に記載。本番適用・pushは未実施。

2026-09-14: 保存失敗時も、画像キーの記録と投稿行の除去を同一D1 batchで行うよう変更。削除側と共通の処理で後片付けし、失敗後は管理画面から再試行できる。既存記録へのキー追加は統合し、処理中に対象が増えた記録を古い処理が消さないよう条件付き削除にした。追加4テストと既存テストの強化により全91テスト成功。新しいmigration・依存変更は不要（前回の0005を共用）。

2026-09-14: 写真削除を `services/delete-post.ts` へ分離。0005で `post_deletions` を追加し、画像キーの保存・公開投稿行の削除を同一D1 batchで実行。R2削除と記録の削除が完了するまで記録を保持し、管理画面から再試行可能にした。DB/R2失敗、応答喪失、同時再試行、HTML/API応答を10テストで検証し、全87テスト成功。本番migration適用・pushは未実施。

2026-09-14: 写真作成を `services/create-post.ts` へ分離。画像を順次保存し、失敗時は対象投稿のDB行をトランザクションで削除してから、書き込みを試みたR2キーを削除。DBの後片付けが失敗したら画像を保持して参照切れを避ける。失敗時は500を返し、投稿ID・対象キー・後片付け失敗段階をログに残す。10件の失敗テストを追加し、9ファイル・77テストとWorker本体/テストの型チェックが成功。削除処理と自動再実行の仕組みは未変更。

2026-09-13: DB定義の基準をSQL migrationへ統一。写真用0000を追加し、旧schema.sqlとテスト専用DDLを削除。Drizzleの制約・日時既定値・過去の未使用列を整合。共通migration初期化と4件の制約/移行テストを追加し、計67テスト成功。実本番DBの履歴照合・適用は未実施。詳細は workers/site-api/DATABASE.md。

2026-09-13: 未使用の旧トップページと、その専用のクラゲ・移動処理・文字アニメーション・音声フック・フッター・アイコン補助・グリッチCSSを削除（9ファイル）。現行デスクトップと雨音、公開ページは維持。旧アイコン用の `simple-icons` はコード上の利用がなくなったが、依存とロックの整理は別の変更で扱う。

2026-09-13: 優先度A-1の全体チェック、CI、通常版のPRビルド検証を実装。GitHub Pagesは利用終了のため関連設定を削除。Bunの既存ロックファイルを維持してCIを構成し、改行をLFへ固定。Bun 1.4.2で固定依存の復元・コードチェック・型チェック・63テストが成功。設定上のWorker名もユーザー指定でsite-apiへ変更。詳細と検証手順は [DEVELOPMENT.md](DEVELOPMENT.md)。以下のチェック結果と指摘は改修前の調査記録。

## 結論

全面的な作り直しは不要。Next.jsの静的サイトとHonoのAPIを維持し、まず自動検証と保存処理の信頼性を固める。その後、画面の責務分離、CSS、実験コードを順に整理する。デスクトップ・読書・写真・南国LPの見た目を共通化する必要はない。

## 初回調査と確認の範囲（改修前の記録）

- フロントの各機能、共有型、Workersのルート・データ層・入力・表示・テスト、ビルド設定、ワークフロー、READMEを確認。
- `npx --no-install biome check src`: 58ファイルを確認し、38件のエラーで失敗。表示された診断は主に整形差分。直近の変更ファイルのチェック成功とは別問題。
- Workersの `vitest run`: 7ファイル・63テストが成功。
- Workersのソースおよびテストの `tsc --noEmit`: 両方成功。
- 対象コミットの本番ビルドは直前の作業で成功している。今回の調査では再ビルドしていない。
- Cloudflare側の実際のAccess設定、稼働中DB、デプロイ結果、実利用時の性能は未確認。以下の障害シナリオはコード上の分析であり、本番で発生したという意味ではない。
- 初回調査時は計画書だけを追加した。その後の実装・検証結果は上の進捗と各項目に記載。

## すでに良い部分

- Workersは `routes → data → serializers / views` に分かれている。再度大規模なフォルダ移動をする必要はない。
- `shared/types/book.ts` と `shared/types/diary.ts` にAPIの共有型がある。型の共有を一から導入する段階ではない。
- 読書機能には `model.ts`、パレット抽出、取得フック、棚、詳細表示の分離がある。
- 雨は非表示タブ・動きを減らす設定・後始末に対応済み。表紙色も遅延取得・キャッシュ上限・失敗時の代替色がある。
- Workersには既存テストがあるため、保存処理の改修を小さく検証して進められる。

## 優先度A: 最初に着手

### 1. リポジトリ全体を検証できる状態にする【実装完了】

実施済み:
- 整形差分を解消し、LFの改行設定と共通チェックコマンドを追加。
- Bun 1.4.2と既存ロックを維持し、固定依存の復元を確認。
- `.github/workflows/ci.yml` でpush/PR時のコード・型チェック・Workerテストを実行。静的ビルドと出力チェックはPR時のみ。
- GitHub Pagesは利用終了のため、専用ワークフロー・コマンド・サブパス設定を削除。
- ビルド時のHTTPS同時接続数を制限し、通常版のビルドと出力チェックが成功。

運用上の残確認: GitHub上のCI実行結果とruleset。Cloudflareの自動デプロイとは独立しており、CI成功を待つ構成にはしていない。CSSの整形導入は項目6に残す。手順は [DEVELOPMENT.md](DEVELOPMENT.md)。

### 2. 写真保存・削除を、途中失敗から回復できる処理にする【保存・削除の失敗対策実装済み／残作業あり】

対象: `workers/site-api/src/services/create-post.ts`、`services/delete-post.ts`、`routes/admin/posts.ts`、`data/post-deletions.ts`、`test/create-post.spec.ts`、`test/delete-post.spec.ts`。

実施済み:
- 保存処理をHTTPルートから分離。投稿ID、画像順、EXIF日時、拡張子由来のMIME、成功レスポンスの形式を維持。
- 画像を順番に保存し、未完了の別アップロードが後片付けの後に完了する競合をなくす。
- R2書き込みを試みたキーを追跡。書き込み完了後の応答失敗も削除対象に含める。
- 保存失敗時はD1画像行・投稿行を同一batchで削除し、成功を確認してからR2を削除。既存の別投稿には触れない。
- DBの後片付け失敗時はR2を保持。後片付け失敗を隠さず、対象ID・キー・段階をログに残し、クライアントへは500を返す。
- 初期DB書き込み、ファイル読み取り、R2書き込み、画像行保存、応答喪失、DB/R2の後片付け失敗、HTTPエラー応答をテスト。全77テスト成功。
- 削除側は `post_deletions` に画像キーを保存し、投稿・画像行を削除するところまでを同一トランザクションにした。DB処理失敗時はR2を削除しない。
- R2が途中で失敗しても投稿はDB上の公開一覧から除かれ、キーの記録は残る。同じ投稿IDで再実行して、画像と記録の削除を完了できる。
- 管理画面に未完了の削除と再試行ボタンを追加。HTMLフォームの失敗画面も再試行可能。APIは失敗時500、成功形式と不存在の404は維持。
- DB/R2の部分失敗と応答喪失、記録削除失敗、同時再試行、別投稿の保持など10テストを追加。この段階では全87テスト成功。
- 保存失敗時の後片付けも永続記録・手動再試行へ統一。記録追加と完了処理の競合も検証。
- 保存予定キーをR2書き込み前に永続化し、新規投稿は全画像保存後に公開。公開APIには内部状態や識別子を含めない。公開済み投稿に関連付いた画像だけ配信する。
- 初回INSERTと公開更新の応答喪失に対応。公開成否の照合まで失敗した場合は、公開済み投稿を誤って消さないよう後片付けを保留する。
- 中断投稿は管理画面から削除可能。画像行に未登録の保存予定キーも削除記録へ移す。
- 検証: **11ファイル・97テスト成功**。コードチェック、フロント・Worker本体・Workerテストの型チェック成功。使い捨てローカルDBへ0000〜0006の適用成功。テストの画像レスポンスを読み切るよう修正し、ストレージ後片付けエラーを解消した。

残作業:
- 削除側の自動再試行は未導入。記録と管理画面からの手動再試行で対応する。既存のCDN・ブラウザキャッシュを即時消去する処理は含めていない。
- DB障害が継続する間は後片付けを保留する。復旧後に未完了投稿または削除記録から手動削除できる。自動検出・再開は未実装。
- デプロイ前に0005・0006を対象DBへ適用する。既存DBの手動適用履歴の照合は項目3の手順に従う。
- 未完了投稿の削除はアップロード終了・中断を確認してから行う。稼働中のR2書き込みと手動削除が重なり、その後Workerが終了すると画像が残る可能性がある。リース等による排他は未導入。
- 識別子はサーバー側の1回の保存処理用。クライアントの重複送信防止・同一リクエストの再送判定は未対応。

完了条件: 途中失敗しても、公開中の壊れた投稿を残さず、再試行または後片付けができる。

### 3. DB定義とテストDBの作り方を揃える【実装・ローカル検証完了】

実施済み:
- SQL migrationを基準にし、不足していた写真テーブルの `0000_create_posts.sql` を追加。既存0001〜0004は変更していない。
- `test/setup.ts` で共通migrationを適用し、テスト専用の手書きDDLを削除。過去の投入データだけを空にして各テストのfixtureを用意する。
- `schema.sql` を削除し、削除処理だけを持つローカル専用 `reset-dev.sql` と共通migrationによる再作成に分離。
- Drizzleの外部キー・UNIQUE・インデックス・日時のSQL式・過去の未使用列 `face_out` を整合。
- `test/migrations.spec.ts` に4テストを追加。実日時の保存、重複拒否、外部キー、削除連動、定義の整合、データを保持した0003→0004移行と再適用を検証。

検証結果: コード・型チェックと **8ファイル・67テストが成功**。使い捨ての新規ローカルD1でもWranglerによる全migration適用が成功。

本番運用の残作業:
- 実DBの構造・制約・`face_out` の有無、`d1_migrations` と手動適用履歴を読み取りで照合する。
- 履歴未登録のDBに一括再適用せず、適用済み記録と不足分への対応を個別に決める。
- 本番データの照合・修正・migration適用・デプロイは未実施。テストで確認したのは履歴を管理したDBの移行であり、本番の現状を保証するものではない。

手順と注意点は [DATABASE.md](workers/site-api/DATABASE.md)。

## 優先度B: 次の数回の改修で進める

### 4. API境界の検証と取得方法を統一する

根拠: `src/components/reading/useBooks.ts:25`、`src/app/diary/page.tsx:12`、`shared/types/`、`workers/site-api/src/inputs/book-form.ts`。

共有型はあるが、JSONを受け取った実行時の検証は弱い。読書は配列かどうかだけ、日記は型アサーション。日記だけ開発時に無条件でモックを使うため、普段の開発で実APIとの不整合を見逃しやすい。写真フォームではファイル数・総容量・日付等、本フォームでは日付・URL・評価の整数性などの扱いを明確にしたい。

進め方:
- APIベースURL、HTTPエラー、タイムアウト、中断処理を小さな共通クライアントにまとめる。
- Book/Diaryのレスポンス検証を共有型の近くに置く。DB行の型をそのまま公開型にはしない。
- モック/APIは明示的な設定で切り替える。
- フォームの検証結果をフィールド別エラーとして返す。
- 表紙色の失敗時代替や取得中断など、既にある良い挙動は維持する。

完了条件: 空データ・不正JSON・通信失敗・再試行を両画面で検証できる。

### 5. 写真モーダルの基本動作を読書側と揃える

根拠: `src/components/diary/PhotoLightbox.tsx:38`、`src/components/reading/BookDrawer.tsx:14`。

写真は `<dialog open>`、読書は `showModal()`・スクロール固定・閉じた後のフォーカス復元。見た目が似ていても操作上の性質が異なる。

進め方: 見た目は共通化せず、モーダルを開く/閉じる、背景の操作抑止、Escape、フォーカス復元だけを揃える。空の写真配列への防御もAPI検証と合わせて行う。

完了条件: キーボードで開閉でき、閉じると元の写真/本に戻り、背景にフォーカスが漏れない。

### 6. デスクトップとCSSを編集しやすくする

根拠: `src/components/desktop/LinuxDesktop.tsx`（271行）、`desktop.css`、`desktop-icons.css`、`src/app/tropical/tropical.css`（約10KBが2行）。

LinuxDesktopにウィンドウ移動、端末コマンド、プロフィール、メニュー、ドックが同居している。CSSは圧縮状態や後付けの上書きが多く、変更の効く範囲が追いにくい。南国LPのCSSも今回の制作で作った改善対象。

進め方:
- 先にCSSを整形。次に不要な旧セレクタを削除し、画面別に閉じる。
- `useWindowPosition`、端末コマンド処理、ProfileWindow、DesktopNavigation程度の単位に分ける。
- アプリ名・アイコン・リンク先を一箇所に定義し、メニューとドックとデスクトップのずれを防ぐ。
- 各ページの色や書体はそのページ内の変数にする。全ページ共通テーマにはしない。
- ドラッグ、最小化、復帰、モバイル切り替えを表示比較と操作確認で守る。

完了条件: プロフィール文・アイコン・端末操作を変更するとき、別の機能を読み解かなくて済む。

### 7. 管理画面のブラウザ用コードを文字列から取り出す

根拠: `workers/site-api/src/views/upload-script.ts`。

EXIF取得、画像縮小、プレビュー、投稿がHTMLへ埋め込む文字列内にあり、ブラウザ側処理に通常の型検証を効かせにくい。exifrも外部URLから読み込む構成。

進め方: 管理画面全体をReact化する必要はない。アップロード処理だけを通常のTypeScriptとしてビルドし、画像変換とEXIF日時変換を単独検証できるようにする。既存の時刻保存仕様を回帰テストで守る。

完了条件: 画像変換失敗・読み込み失敗・送信失敗をテスト可能にし、画面から再試行できる。

## 優先度C: 運用や機能拡大に合わせる

### 8. 記事・写真・本の件数増加に備える

写真は `getRecentPosts(limit = 50)` に固定され、古い記録へのページ送りがない。本は全件取得。microCMSの一覧はフロント・Workerとも100件指定だが、次ページの取得処理はない。

まず写真を日付とIDによるカーソルページングにする。本は件数や通信量を測定してからサーバー検索へ移す。ブログは101件以上を含めた静的ページ生成を確認する。全件が少ない間に仮想リストや検索基盤を導入する必要はない。

### 9. ブログのデータ経路と本文形式を決める

根拠: `src/lib/microcms.ts`、`src/data/blog-posts.ts:27`、`workers/site-api/src/routes/blog.ts`。

Next.jsのビルド時SDK取得とWorkerのプロキシ取得が並立し、現在のフロントは前者を使用。本文はリッチテキストHTMLを正規表現で削り、Markdownへ再変換しているため、リンクや画像等の表現を失う可能性がある。

静的生成と下書きプレビューの役割を決め、本文をHTMLとして扱うのかMarkdownとして扱うのか統一する。HTML出力については許可する表現と信頼境界も明記する。WorkerのブログAPIは、外部利用やプレビュー用途を確認してから存廃を判断する。

### 10. ミニアプリの計算・描画・UIを分ける

- `Microscope.tsx`（995行）: 生物モデル、描画、操作、ロード処理が一つのeffect内。モデル/描画/操作に分割する。CDNロードの失敗処理と、アンマウント後にロードが完了する場合の生成抑止を先に直す。
- `PhysicsTextGrid.tsx`（299行）: 物理世界の生成・破棄と、画面操作を分ける。
- `text-delta/page.tsx`（379行）: 差分計算を純粋関数へ。LCSの二次元配列は入力長の積で増えるため、大きな入力への上限や別スレッド化は計測して判断する。
- `VoidSpace.tsx`: RAFやgeometry/materialの後始末はあるがrendererのdisposeはない。非表示時停止、描画密度の上限、時間ベースの速度を検討する。

将来Blender作品を埋め込むなら、重い表示の遅延ロード、失敗時表示、タブ非表示時の停止をこの段階で整える。今から汎用3Dエンジンを作る必要はない。

### 11. 未使用コードと実験ページの置き場所を整理する【一部完了】

完了: 未参照の `LegacyHome.tsx` と、専用のクラゲ・移動処理・文字アニメーション・旧音声処理・フッター・アイコン補助・グリッチCSSの計9ファイルを削除。現在のデスクトップと雨音は維持し、コード・型チェック・ビルド・出力チェックが成功。

残作業: 利用がなくなった `simple-icons` などの依存整理。依存やロックの変更はまだ行っていない。

一方、`/tropical/` や既存ミニアプリは利用可能なページなので、未使用扱いで消さない。今後のデモを通常ページとして残すか、実験用の導線にまとめるか決める。ファイル移動だけで既存URLを変えない。

### 12. 設定・文書・運用前提を揃える

- 完了: READMEをCloudflareのGitHub連携による自動デプロイへ更新。GitHub Pages関連設定を削除。
- `NEXT_PUBLIC_DIARY_API` は読書にも使うため、互換期間を設けて `NEXT_PUBLIC_SITE_API` などへ移行する。
- `next.config.mjs` 相当の設定ではbuild領域と静的出力の両方にoutを使用。出力ディレクトリの役割を分け、型チェック対象も明確にする。
- 静的素材パスの構築を揃える。GitHub Pagesのサブパス対応は廃止済みなので再導入しない。
- フォントがルートlayoutへ集まっている。ページ別の必要性と転送量を確認して配置を見直す。
- 完了: Bunの既存ロックを維持した復元・検証手順を明記。依存管理ツールの変更は行わない。
- Access依存を運用文書とデプロイ検証に含める。現設定はworkers.dev/previewを無効化しているため、公開されていると断定しない。必要に応じてWorker内検証も別途設計する。
- Cloudflareの既存Worker/D1/R2リソース名は、見た目の統一だけを理由に変更しない。ソース側のsite-api命名は維持する。

## 推奨する着手順とPR単位

1. 重複送信と自動回復、稼働中アップロードと削除の競合対策。公開前状態・予定キーの記録・手動回復は実装済み。
2. API取得の共通化と入力/レスポンス検証 → 写真モーダルの基本動作。
3. CSS整形 → デスクトップの責務分離 → 残っている未使用依存の整理。
4. 投稿数や実利用に合わせてページング、ブログ経路、ミニアプリの性能を改善する。

自動検証基盤とDB定義・テストDBの統一は実装済みのため、着手待ちの一覧から外した。本番DBの照合・移行判断は項目3の運用残作業として追跡する。

各PRで「変える挙動」「変えない挙動」「確認方法」を書く。見た目だけの変更とDB移行を同じPRに混ぜない。フォルダ分割・ライブラリ追加そのものを目的にしない。
